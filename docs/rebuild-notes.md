# Debug APK rebuild notes

Build target: debug APK with saved Cefrium AC3/EAC3 runtime.

Requirements:
- Keep assembleDebug.
- Use saved Cefrium AAR runtime.
- Preserve full Lampa frontend behavior.
- Do not add probe-only defaults to production build.
